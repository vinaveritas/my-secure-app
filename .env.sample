# --- App ---
PORT=3000
JWT_SECRET=change-me-later

# --- Database (used by Docker Postgres) ---
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgrespw
POSTGRES_DB=vinaveritas_db

# --- App <-> DB connection inside Docker network ---
DATABASE_URL=postgresql://postgres:postgrespw@db:5432/vinaveritas_db
